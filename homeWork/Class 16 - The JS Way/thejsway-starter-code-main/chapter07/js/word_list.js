/*
List of words
*/

// Write a program that asks the user for a word until the user types "stop". The program then shows each of these words, except "stop".

    // const words = []

    // let addWord

    //WHILE

    //     while (addWord !== "stop") {
    //         addWord = prompt("Enter Stop").toLowerCase()
    //         words.push(addWord)
    //         if (addWord === "stop") {
    //             words.pop()
    //         }    
    //     }
    
        // console.log(words)

    //FOR

    //     for (let i = 0; addWord !== "stop"; addWord = prompt("Enter Stop").toLocaleLowerCase()) {
    //         words.push(addWord)
    //         if (addWord === "stop" || addWord === undefined) {
    //             words.pop()
    //             words.shift()
    //         }
    //     }

        // console.log(words)
