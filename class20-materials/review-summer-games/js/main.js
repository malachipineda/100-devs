//Create a function that takes in an array of numbers. Multiply each number together and alert the product. 

function arrayOfNumbers(array) {
    let product = 1    
    array.forEach((number, idk) => (product *= number))
        alert(number,idk)
    }

arrayOfNumbers([1, 2, 3, 4])